# japananh/homebrew-tap

Third-party Homebrew tap for tools published by @japananh.

## Install

```bash
brew tap japananh/tap
brew install --cask aimonitor
```

The `Casks/` and `Formula/` directories are auto-managed by goreleaser from each upstream repo. Do not edit by hand — your changes will be overwritten on the next release.
